# Push

変更されているファイルを確認し、論理的な変更単位ごとに個別にコミットしてからまとめてプッシュしてください。

手順:

1. `git diff` と `git status` で変更内容を把握する
2. 関連するファイルをグループ化し、1グループ＝1コミットとする
3. 各グループを `git add <files>` → `git commit` で順番にコミットする
4. すべてのコミットが終わったら `git push` する

コミットメッセージは各変更の内容から適切に生成してください。
